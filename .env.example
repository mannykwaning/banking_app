# Environment Configuration Example
# Copy this file to create environment-specific configurations:
# - .env.development (for local development)
# - .env.test (for testing)
# - .env.production (for production)
# Or simply copy to .env for default configuration

# Environment (development, test, production)
ENVIRONMENT=development

# Database Configuration
DATABASE_URL=sqlite:///./data/banking.db

# Application Configuration
APP_NAME=Banking App API
APP_VERSION=1.0.0
DEBUG=True

# API Configuration
API_V1_PREFIX=/api/v1

# Security
# IMPORTANT: Use a strong secret key in production
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-in-production

# Transfer Limits Configuration
# These control money transfer limits and validation
MAX_TRANSFER_AMOUNT=100000.0          # Maximum single internal transfer amount
DAILY_TRANSFER_LIMIT=500000.0         # Maximum total daily transfer amount per account
MIN_TRANSFER_AMOUNT=0.01              # Minimum transfer amount
MAX_EXTERNAL_TRANSFER_AMOUNT=50000.0  # Maximum single external transfer amount
MIN_ACCOUNT_BALANCE=0.0               # Minimum account balance after transfer

# Logging Configuration
LOG_LEVEL=INFO
LOG_DIR=logs

# CORS Configuration (JSON array format)
CORS_ORIGINS=["http://localhost:3000","http://localhost:8080"]
